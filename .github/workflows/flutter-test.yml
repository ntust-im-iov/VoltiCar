name: Flutter Test (Self-hosted)

on:
  push:
    branches: ['feature/*', 'develop', 'main']
  pull_request:
    branches: [main, develop]


jobs:
  block-feature-to-main:
    name: Block feature/* -> main PR
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Check source and target branches
        run: |
          echo "🔍 檢查是否為不允許的合併路徑..."

          HEAD_REF="${{ github.head_ref }}"
          BASE_REF="${{ github.base_ref }}"

          echo "來源分支: $HEAD_REF"
          echo "目標分支: $BASE_REF"

          if [[ "$HEAD_REF" =~ ^feature/ && "$BASE_REF" == "main" ]]; then
            echo "❌ 違規合併！正在發送 Discord 通知..."

            curl -H "Content-Type: application/json" \
              -X POST \
              -d "{
                \"embeds\": [{
                  \"title\": \"❌ 違規合併阻擋：feature/* → main\",
                  \"description\": \"\
                  🔁 **來源分支**：\`$HEAD_REF\`\n\
                  📥 **目標分支**：\`$BASE_REF\`\n\
                  👤 **作者**：\`${{ github.actor }}\`\n\n\
                  🚫 此路徑不允許合併！請改合併至 \`develop\` 分支。\n\
                  🔗 [查看 PR 詳情](${{ github.event.pull_request.html_url }})\",
                  \"color\": 16711680
                }]
              }" \
              $DISCORD_WEBHOOK

            exit 1
          else
            echo "✅ 合併路徑合法"
          fi
  test:
    needs: block-feature-to-main
    name: Run Flutter Tests on Self-Hosted
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Flutter
        run: flutter --version

      - name: Get dependencies
        run: flutter pub get

      - name: Run Dart Analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings > analyze_report.txt || true
        
      - name: Upload Analysis Report as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: flutter-analyze-report
          path: analyze_report.txt

      - name: Run tests
        run: flutter test

      - name: Notify Discord
        if: always()
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          STATUS="${{ job.status }}"
          COLOR="16777215" # default: gray
      
          if [ "$STATUS" == "success" ]; then
            COLOR="65280" # green
            EMOJI="✅"
            STRING="可以下班了"
          elif [ "$STATUS" == "failure" ]; then
            COLOR="16711680" # red
            EMOJI="❌"
            STRING="繼續加班"
          else
            EMOJI="⚠️"
            STRING="繼續加班"
          fi
      
          curl -H "Content-Type: application/json" \
            -X POST \
            -d "{
              \"embeds\": [{
                \"title\": \"${EMOJI} Flutter CI 執行結果：**${STATUS}**\",
                \"description\": \"\
          🔁 **分支**：\`${{ github.ref_name }}\`\n\n\
          👤 **作者**：\`${{ github.actor }}\`${STRING}\n\n\
          🧪 **Workflow**：[點我查看執行情況](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})\",
                \"color\": ${COLOR}
              }]
            }" \
            $DISCORD_WEBHOOK


